
// Set is nothing but the collection of distinct values that means values will not repeated
let nums = new Set("bookskeeper");
console.log(nums);
//if we want to serch  a elements in set... there is method 'has'

console.log(nums.has('o'));